import { InjectionToken } from "@angular/core";

/**
 * An arbitrary username for the chat, for development purposes
 */
export const USERNAME = new InjectionToken<string>('An arbitrary username for the chat, for development purposes');